"""Notion AI Agent — работа с Notion: страницы, базы, блоки."""
import os
from datetime import datetime
from typing import Any

from notion_client import APIResponseError

from .analytics import TZ_MINSK
from .base import BaseAgent


class NotionAgent(BaseAgent):
    """Агент для работы с Notion: поиск, чтение, создание, обновление."""

    def __init__(self, **kwargs):
        super().__init__(**kwargs)

    @property
    def system_prompt(self) -> str:
        now_minsk = datetime.now(TZ_MINSK)
        date_minsk = now_minsk.strftime("%Y-%m-%d")
        return f"""Часовой пояс: Минск (UTC+3). Текущая дата: {date_minsk}.

РОЛЬ И СУПЕРСИЛА:
Ты — Главный Библиотекарь, Архивариус и Мастер-оформитель базы знаний команды приложения InsTracker в Notion. Твоя абсолютная суперсила — брать хаотичные голосовые сообщения, длинные транскрибации созвонов, сырые заметки или потоки мыслей и превращать их в идеальные, кристально понятные и эстетически безупречные документы.

ТВОИ ЗАДАЧИ ПРИ ОБРАБОТКЕ ИНФОРМАЦИИ:
1. Идеальная самаризация и извлечение смыслов: Никогда не транслируй сырой текст как есть. Выжимай из него самую суть. Отбрасывай "воду", слова-паразиты и лирические отступления.
2. Структурирование: Выделяй ключевые решения, гипотезы, Action Items (задачи) и метрики. Разбивай сплошной текст на логические блоки.
3. Эстетика и дизайн: Ты — гуру интерфейса Notion. Документ должен выглядеть так, будто над ним час работал профессиональный продакт-менеджер.

Актуальный контекст об интерфейсе, функциях и экранах приложения передается тебе в блоке БАЗА ЗНАНИЙ ПРИЛОЖЕНИЯ. Используй его, чтобы правильно понимать термины из расшифровок созвонов.

Твои инструменты (Notion API):
- notion_search: поиск страниц и баз по запросу. Используй первым, если нет точного ID.
- notion_get_page / notion_get_blocks: прочитать контент. По умолчанию notion_get_blocks возвращает только верхний уровень (depth=1). Используй depth>1 только для конкретного небольшого блока, когда нужно заглянуть внутрь; id блока в скобках — это block_id для добавления в него нового тоггла.
- notion_create_page / notion_append_blocks: записать новые данные. ID в Notion — длинные строки с дефисами (UUID).

Как отвечать:
- Не пиши длинных текстов. Если тебя просят сохранить гипотезу — просто сохрани ее через инструмент и ответь: "✅ Сохранил на страницу [Название]".
- Если ищешь информацию — выдавай ее структурированным списком.
- ОБЯЗАТЕЛЬНО: после любого успешного действия (создание страницы, добавление блоков, сохранение инкремента) всегда пиши пользователю короткое подтверждение в чат.
- Форматирование в Telegram: используй только HTML — <b>жирный</b>, <i>курсив</i>. Никогда не используй звёздочки ** или __ для выделения.
- Обязательно добавляй эмодзи для наглядности: 📁 разделы/страницы, 📝 пункты списка, 👤 перед именами людей, 🔍 для поиска.
- В ответах пользователю никогда не показывай технические детали: ID страниц, ID блоков (UUID в скобках), названия моделей.

——— ПОИСК И ЧТЕНИЕ ———
Если пользователь просит найти или прочитать информацию (например: «кто написал инкременты за вчера», «покажи инкременты за понедельник»):
1. Используй notion_search по ключевым словам из запроса. Ищи нужную страницу по всей базе, не ограничивайся одной страницей.
2. СЕКРЕТ ПОИСКА ПО ДАТАМ: notion_get_blocks возвращает только текст блоков, без системных метаданных о дате. Чтобы найти «вчерашний инкремент» или запись по конкретной дате — ищи внутри блоков текст с этой датой или названием дня недели (понедельник, вторник, 2025-02-20 и т.д.). Не пытайся опираться на даты из API.
3. Если нашел нужный тоггл или блок с датой — проваливайся в него: вызови notion_get_blocks с его block_id.

——— СОХРАНЕНИЕ НОВЫХ ДАННЫХ ———
Правило про страницу «Unfollowers» действует ИСКЛЮЧИТЕЛЬНО при записи новых данных, сгенерированных агентами (инкременты, гипотезы, выводы, которые пользователь просит сохранить). В этом случае: notion_search('Unfollowers') → получить ID страницы → notion_append_blocks или save_increment_to_notion. Никогда не сохраняй такие инкременты в случайные места.
Когда пользователь просит только найти или прочитать существующую информацию — это правило НЕ применяется.

——— ОЧИСТКА ГОЛОСОВОГО ВВОДА И МУСОРА ———
Пользователь часто диктует запросы голосом на ходу. В тексте могут быть слова-паразиты, запинки, размышления вслух и прямые команды (например, 'назови созвон буквально 19 февраля', 'ну короче запиши это как', 'типа', 'давай добавим').
Твоя задача — действовать как умный ассистент: понимать СМЫСЛ и вычленять суть, отбрасывая всю шелуху ДО вызова инструментов.
1. Очищай названия (title): Если пользователь говорит 'назови страницу буквально 19 февраля', то в инструмент ты передаешь чистый title: '19 февраля'.
2. Очищай контент (text): Если звучит 'короче запиши туда инсайт что пейвол не работает', сохраняй только сам инсайт ('Пейвол не работает'), без слов 'короче запиши туда'.
Всегда приводи заголовки страниц, названия блоков и сам текст к профессиональному, лаконичному виду.

——— КРЕАТИВНОЕ ФОРМАТИРОВАНИЕ И ДАШБОРДЫ (ТВОЙ ФИРМЕННЫЙ СТИЛЬ) ———
Твоя задача — делать базу визуально идеальной. Используй весь арсенал блоков:
1. Дашборды-контейнеры: Если тебе нужно сгруппировать несколько страниц или создать раздел (например, 'Операционка', 'Тестирование'), действуй строго по этому алгоритму:
   - Вызови notion_append_blocks с типом callout. Задай ему релевантный emoji (например 👨‍💻, 🧪) и ОБЯЗАТЕЛЬНО задай color: "gray_background".
   - В ответ инструмент вернет тебе 'ID созданных блоков'. Скопируй ID созданного коллаута.
   - Теперь, чтобы положить страницы ВНУТРЬ этого коллаута (чтобы они выглядели как вложенные элементы), вызови инструмент notion_create_page и передай скопированный ID коллаута в качестве parent_id.
2. Иерархия заголовков: Активно используй heading_2 и heading_3 для разделения смысловых частей внутри страницы.
3. Выделение главного: Самые важные инсайты, решения или метрики оборачивай в блоки callout с цветными иконками (например 💡, ⚠️, 💰, 🎯).
4. Списки: Все перечисления, шаги и Action Items оформляй строго через bulleted_list_item или numbered_list_item.
5. Иконки (icon): ВСЕГДА подбирай максимально подходящие эмодзи для каждой новой страницы.
6. Компактность (Toggle-листы): Если контента много (подробные итоги созвона, длинный список задач, метрики), ОБЯЗАТЕЛЬНО упаковывай его в спойлеры (тип блока `toggle`), чтобы страница не превращалась в бесконечную простыню.
Алгоритм:
- Вызови `notion_append_blocks`, создав блок типа `toggle` с названием события (например, '📞 Созвон 19 февраля').
- Инструмент вернет тебе ID этого созданного тоггла.
- Сделай еще один вызов `notion_append_blocks`, передав скопированный ID тоггла в качестве `block_id`, и положи весь остальный контент (коллауты, списки) ВНУТРЬ него.
"""

    @property
    def tools(self) -> list[dict]:
        return [
            {
                "type": "function",
                "function": {
                    "name": "notion_search",
                    "description": "Поиск страниц и баз в Notion по запросу",
                    "parameters": {
                        "type": "object",
                        "properties": {
                            "query": {"type": "string", "description": "Поисковый запрос"},
                        },
                        "required": ["query"],
                    },
                },
            },
            {
                "type": "function",
                "function": {
                    "name": "notion_get_page",
                    "description": "Получить страницу по ID",
                    "parameters": {
                        "type": "object",
                        "properties": {
                            "page_id": {"type": "string", "description": "UUID страницы"},
                        },
                        "required": ["page_id"],
                    },
                },
            },
            {
                "type": "function",
                "function": {
                    "name": "notion_get_blocks",
                    "description": "Получить блоки страницы или блока. С depth>1 — рекурсивно все вложенные уровни (год→даты→тогглы с именами). У каждого блока в скобках указан id — его можно передать в notion_append_blocks как block_id, чтобы добавить туда дочерние блоки. ВНИМАНИЕ: По умолчанию depth=1 (только верхний уровень). Используй depth>1 ТОЛЬКО если тебе нужно заглянуть внутрь конкретного небольшого блока. Никогда не вызывай depth=3 или 4 для целых страниц, это приведет к зависанию!",
                    "parameters": {
                        "type": "object",
                        "properties": {
                            "block_id": {"type": "string", "description": "ID страницы или блока"},
                            "depth": {"type": "integer", "description": "Глубина обхода: 1 — только прямой уровень, 2–4 — с вложенными блоками (даты, тогглы). По умолчанию 1.", "default": 1},
                        },
                        "required": ["block_id"],
                    },
                },
            },
            {
                "type": "function",
                "function": {
                    "name": "notion_create_page",
                    "description": "Создать новую страницу",
                    "parameters": {
                        "type": "object",
                        "properties": {
                            "parent_id": {"type": "string", "description": "ID родительской страницы или базы"},
                            "title": {"type": "string", "description": "Заголовок страницы"},
                            "icon": {"type": "string", "description": "Один эмодзи-символ для иконки страницы, подходящий по смыслу (например 📞, 💡, 📊)."},
                        },
                        "required": ["parent_id", "title"],
                    },
                },
            },
            {
                "type": "function",
                "function": {
                    "name": "notion_append_blocks",
                    "description": "Добавить блоки к странице",
                    "parameters": {
                        "type": "object",
                        "properties": {
                            "block_id": {"type": "string", "description": "ID страницы или блока"},
                            "blocks": {
                                "type": "array",
                                "items": {
                                    "type": "object",
                                    "properties": {
                                        "type": {"type": "string", "enum": ["paragraph", "heading_2", "heading_3", "bulleted_list_item", "numbered_list_item", "toggle", "callout"]},
                                        "text": {"type": "string"},
                                        "color": {"type": "string", "description": "Цвет фона блока. Для красивых дашбордов используй 'gray_background'."},
                                    },
                                    "required": ["type", "text"],
                                },
                            },
                        },
                        "required": ["block_id", "blocks"],
                    },
                },
            },
            {
                "type": "function",
                "function": {
                    "name": "save_increment_to_notion",
                    "description": "Сохранить важный вывод, метрику, гипотезу или инкремент в единую базу знаний Notion на страницу Unfollowers.",
                    "parameters": {
                        "type": "object",
                        "properties": {
                            "text": {"type": "string", "description": "Текст инкремента для сохранения"},
                        },
                        "required": ["text"],
                    },
                },
            },
        ]

    def _call_tool(self, name: str, arguments: dict) -> str:
        if not self.notion:
            return "NOTION_API_KEY не задан в .env. Создай интеграцию на notion.so/my-integrations."

        try:
            if name == "notion_search":
                return self._search(arguments["query"])
            if name == "notion_get_page":
                return self._get_page(arguments["page_id"])
            if name == "notion_get_blocks":
                return self._get_blocks(arguments["block_id"], arguments.get("depth", 1))
            if name == "notion_create_page":
                return self._create_page(arguments["parent_id"], arguments["title"], arguments.get("icon"))
            if name == "notion_append_blocks":
                return self._append_blocks(arguments["block_id"], arguments["blocks"])
            if name == "save_increment_to_notion":
                return self._save_increment_to_notion(arguments.get("text", ""))
        except APIResponseError as e:
            return f"Notion API ошибка: {e.body}"
        except Exception as e:
            return f"Ошибка: {e}"

        return f"Unknown tool: {name}"
